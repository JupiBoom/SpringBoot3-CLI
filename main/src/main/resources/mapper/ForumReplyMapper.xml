<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rosy.main.mapper.ForumReplyMapper">

    <resultMap id="ForumReplyVOResultMap" type="com.rosy.main.domain.vo.ForumReplyVO">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="parent_id" property="parentId"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="user_avatar" property="userAvatar"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByPostId" resultMap="ForumReplyVOResultMap">
        SELECT fr.id, fr.post_id, fr.parent_id, fr.user_id, fr.user_name,
               u.avatar as user_avatar, fr.content, fr.like_count, fr.status, fr.create_time
        FROM forum_reply fr
        LEFT JOIN user u ON fr.user_id = u.id
        WHERE fr.post_id = #{postId} AND fr.is_deleted = 0 AND fr.status = 1
        ORDER BY fr.create_time ASC
    </select>

</mapper>
